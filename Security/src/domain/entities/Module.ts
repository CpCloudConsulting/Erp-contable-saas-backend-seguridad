
export class Module {
  constructor(
    public id: number,
    public nombre: string,
    public is_active: boolean
  ) {}
}

export class Subscription {
  constructor(
    public idEmp: number,
  ) {}
}